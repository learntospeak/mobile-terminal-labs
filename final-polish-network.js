(function(){
var e=window.ScenarioEngine;if(!e||!Array.isArray(e.scenarios))return;
function find(id){return e.scenarios.find(function(s){return s&&s.id===id})}
function add(step,cmd,msg){step.exploration=Array.isArray(step.exploration)?step.exploration:[];step.exploration.push({match:{command:cmd},feedback:msg})}
function patch(id){var s=find(id);if(!s||s.__networkPolish)return;s.__networkPolish=true;(s.steps||[]).forEach(function(st){var fam=String(st.commandFamily||'').toLowerCase();if(['ping','tracert','pathping','nslookup','ipconfig','getmac','arp','route','netstat','findstr'].indexOf(fam)>=0){add(st,'ping','Good reachability check. If reachability is already proven, move to the next evidence step.');add(st,'nslookup','Good DNS check. If name resolution is already proven, continue with the next task.');add(st,'ipconfig','Good local network check. If adapter details are known, continue with the next diagnostic step.');add(st,'netstat','Good port evidence. If you already found the target port, continue with filtering or documentation.');add(st,'findstr','Good filter. If the important line is already found, continue with the note or verification.');}if(String(st.objective||'').toLowerCase().indexOf('verify')>=0){add(st,'dir','Good destination check. If the note already exists, open it to verify.');add(st,'type','Good verification. If this opens the expected note, the task is effectively complete.');}})}
['win-ping-fileserver','win-ipconfig-and-getmac-audit','win-nslookup-fileserver','win-tracert-and-pathping-web-lab','win-arp-and-route-review','win-netstat-connection-audit','win-localhost-service-check','win-web-port-triage','win-dhcp-basic-check','win-basic-firewall-check','win-printer-basic-triage'].forEach(patch);
})();