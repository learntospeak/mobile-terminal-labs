# Workflow Note

Always create a backup before making changes.

- Create a timestamped backup copy in `subnetting game backups`.
- Create a backup git branch before editing files.
- Only start code changes after the backup is saved.

## Future Idea

TODO: Consider first-time command explainers with Patch, using lightweight in-app CSS/JS animation rather than video files. Example: the first ping lesson could explain that ping sends a small test message to another device and waits for a reply, with a replay option added later.
